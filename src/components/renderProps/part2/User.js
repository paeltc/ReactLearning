import React, { Component } from 'react'

class User extends Component {
    render() {
        return (
            <div>
              {/*this.props.name} {/*Render as a props */}
              {/*this.props.name()} {/*Render as a function */}
              {/*this.props.name(true)} {/*Render as a parameter */}
              {/*this.props.render(true)} {/*Render as a parameter */}
            </div>
        )
    }
}

export default User
